# Bridge Gateway 环境变量示例
#
# 1) 复制为 .env 并填写密钥
# 2) 本地构建运行：
#    docker compose -f docker-compose.develop.yml --env-file .env up -d
# 3) 使用已发布镜像运行：
#    IMAGE_TAG=latest docker compose -f docker-compose-deploy.yml --env-file .env up -d
# 4) 如需只构建/推送镜像，可用 docker-compose.images.yml

# 用于签发 Agent Token 的密钥（必填）
# 请使用系统 API POST /system/secret-key/generate 生成随机值
SECRET_KEY=

# 保护 /internal/bridge/* 接口的共享 Token（强烈建议设置）
BRIDGE_GATEWAY_INTERNAL_TOKEN=

# 可选：镜像标签（默认 latest）
# IMAGE_TAG=latest
